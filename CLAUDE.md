# CLAUDE.md — 特定技能 定期面談 管理システム

Claude Code へ：このファイルはプロジェクトの前提です。作業前に必ず読んでください。

## これは何か
介護施設に配属された特定技能1号外国人の「定期面談報告書（参考様式第5-5号）」を、
3ヶ月ごとに記録・管理する **Google Apps Script (GAS) ウェブアプリ**。
データは Google スプレッドシート、デプロイ/同期は **clasp** を使う。

- **各施設ページ**：施設ごとの専用URL（`?fid=施設ID&token=...`）。ログイン不要。自施設の対象者だけ表示。
- **管理ページ（ホップ社内）**：パラメータなしでアクセス→Googleログインのメールを管理者マスタと照合。面談入力・人材追加。

## 唯一の設定
`Code.gs` 冒頭の `SPREADSHEET_ID` に、対象スプレッドシートのIDを入れる。それ以外の設定は不要。

## ファイル構成
| ファイル | 役割 |
|---|---|
| `Code.gs` | サーバー本体（ルーティング・認可・シート読み書き） |
| `admin.html` | 管理ページ（全施設・入力フォーム） |
| `facility.html` | 施設ページ（自施設のみ・閲覧） |
| `styles.html` | 共通CSS（include） |
| `appjs.html` | 共通クライアントJS：描画・面談票モーダル（include） |
| `denied.html` | 無効リンク／PIN入力 |
| `appsscript.json` | マニフェスト（clasp push対象。webapp=匿名を既定） |

HTMLは `include()` で結合。`facility`/`admin`/`denied` はテンプレート（`<?!= data ?>` にJSON注入）。

## データモデル（スプレッドシートの5シート＋ログ）
`管理者マスタ` / `施設マスタ`（アクセストークン・PIN・面談周期を保持） /
`人材マスタ` / `面談記録`（様式の18項目を列で保持・追記型） / `法令違反記録`（記録IDで紐付け） /
`アクセスログ`（自動生成）。18項目のコードは `Code.gs` の `ITEMS` とスプレッドシートの列名が一致している必要がある。

## 絶対に守る設計ルール（重要）
**デプロイは必ず2つに分ける。**
- 施設用：実行=自分／アクセス=**全員（匿名）** … 専用URL配布用
- 管理用：実行=自分／アクセス=**Googleアカウントを持つ全員** … 社内のみ

これを1つの匿名デプロイにまとめると、全施設のPII・法令違反情報が誰でも見えてしまう。
書き込み関数（`addRecord`/`addWorker`/`rotateToken`）は `requireAdmin_()`（Googleログイン照合）必須で、
匿名の施設ページからは呼べない構造にしてある。この不変条件を壊さないこと。

## clasp ワークフロー
```bash
clasp login              # 初回のみ
clasp create --type webapp --title "特定技能 面談管理"   # or: clasp clone <scriptId>
clasp push               # 全ファイル同期（SPREADSHEET_ID 設定後）
clasp open               # UIでデプロイ2つを作成
```

## 現状
- 全ファイル作成済み・構文チェック済み。サンプルデータで動作確認可能。
- TURNER ELIZABETH（F001）はアップロード書類の実データ（居住費控除の法令違反）を反映。
- **未実施**：スプレッドシートID設定、clasp push、2デプロイ作成。

## バックログ（次にやると良い）
- 管理ページに**トークン再発行ボタン**（`rotateToken` は実装済み。UI導線を足すだけ）
- 面談記録から**様式第5-5号のPDF自動生成**
- 履歴を使った**項目別の問題発生分析**（どの面談項目で問題が多いか）
